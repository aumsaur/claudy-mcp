<Window x:Class="PetOverlay.QueueMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message Claude"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MaxWidth="360"
        WindowStartupLocation="Manual">
    <Border Background="#1E1E22" CornerRadius="14" Padding="16" BorderBrush="#46464E" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="Black"/>
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="Message Claude" Foreground="Gray" FontSize="11" Margin="0,0,0,4"/>
            <TextBlock Text="They'll see this at the start of their next turn - not instantly."
                       Foreground="Gray" FontSize="10" TextWrapping="Wrap" Margin="0,0,0,10"/>
            <TextBox x:Name="MessageBox" Padding="6" Margin="0,0,0,10" MinWidth="260" AcceptsReturn="False"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancel" Padding="14,6" Margin="0,0,8,0"
                        BorderThickness="0" Background="#37373C" Foreground="White"/>
                <Button x:Name="SubmitButton" Content="Queue it" Padding="14,6"
                        BorderThickness="0" Background="#3C78DC" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
