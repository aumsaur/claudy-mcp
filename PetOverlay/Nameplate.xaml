<Window x:Class="PetOverlay.Nameplate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">
    <Border x:Name="RootBorder" Background="#E0101018" BorderBrush="#40FFFFFF" BorderThickness="1"
            CornerRadius="5" Padding="7,2" ToolTipService.InitialShowDelay="150">
        <TextBlock x:Name="NameText" Foreground="White" FontSize="11" FontWeight="SemiBold"
                   HorizontalAlignment="Center"/>
    </Border>
</Window>
