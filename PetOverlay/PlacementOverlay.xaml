<Window x:Class="PetOverlay.PlacementOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Cursor="Cross"
        WindowStartupLocation="Manual">
    <!-- Background must be a real brush (not null) for the whole canvas to be
         click-target-able, same reasoning as ToyMarker's RootBorder. -->
    <Canvas x:Name="RootCanvas" Background="Transparent">
        <Image x:Name="GhostImage" Width="32" Height="32" Opacity="0.65"
               IsHitTestVisible="False"
               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
    </Canvas>
</Window>
